# Analysis-Utilities

C++ utilities for analysis of nuclear measurement data, built on
[ROOT](https://root.cern/), with a focus on ergonomics and performance.

Supports CAEN digitizers via CoMPASS and WaveDump acquisition software, plus
SOL-format data from the [SOLARIS DAQ](https://github.com/goluckyryan/SOLARIS_DAQ).
The RooFit-based photopeak fitting backend includes CUDA kernels for its custom
PDFs and dispatches `doEval` to the GPU when built with CUDA support. The flake
also exposes a Python package: a bridge that makes the whole C++ API usable from
PyROOT, and a loader for reading ROOT TTrees into numpy arrays and pandas
DataFrames.

> **Warning**
> Currently in active development; breaking changes are all but guaranteed.

## Documentation

**→ [docs.ethanwtodd.com/analysis-utilities](https://docs.ethanwtodd.com/analysis-utilities/)**

Guides and the full API reference live there. Start with
[Installation](https://docs.ethanwtodd.com/analysis-utilities/installation.html), or jump to
[Photopeak fitting](https://docs.ethanwtodd.com/analysis-utilities/fitting.html),
[GPU acceleration](https://docs.ethanwtodd.com/analysis-utilities/cuda.html), or the
[Python package](https://docs.ethanwtodd.com/analysis-utilities/python.html).

## Quick start

Requires [Nix](https://nixos.org/download/) with
[flakes enabled](https://nixos.wiki/wiki/Flakes#Enable_flakes_permanently).
In a new project directory:

```bash
# C++ template (default) — ROOT + compiled libraries
nix flake init -t github:ewtodd/Analysis-Utilities --refresh
nix develop
```

```bash
# Python template — adds the Python package and ML libraries
nix flake init -t github:ewtodd/Analysis-Utilities#python --refresh
nix develop
```

Compile local code with the included Makefile and run macros with
`root -l macro.cpp+`.

## Repository layout

| Path | Contents |
|------|----------|
| `include/`, `src/` | the C++ library |
| `gpu/` | CUDA kernels for the custom RooFit PDFs |
| `python/` | the `analysis-utilities` Python package |
| `doc/pages/` | narrative documentation, rendered by Doxygen |
| `doc/theme/` | Kanagawa palette for the generated site |
| `checks/` | review checklists for AI-assisted commits |

## Building the docs locally

`doxygen` and `graphviz` are already in both dev shells:

```bash
make docs                              # or:
cmake --build build --target docs
```

Output lands in `docs/html/index.html` (gitignored), warnings in
`docs/doxygen-warnings.log`. `nix build .#docs` produces the same site as a
store path, which is what [docs.ethanwtodd.com/analysis-utilities](https://docs.ethanwtodd.com/analysis-utilities/)
serves; that build fails if doxygen emits any warning.

## AI-assisted development disclosure

Parts of this codebase — primarily the interactive GUI editor and routine
boilerplate — were written with the help of
[Claude Code](https://claude.ai/claude-code) and/or
[son-of-anton](https://github.com/ewtodd/son-of-anton).

All changes to the core analysis logic (fitting models, signal processing,
result extraction) are human-reviewed and approved before being committed.
Unlike son-of-anton, this project actually is for my day job, so a large part of
the code base was directly written by me. The `checks/REVIEW-*.md` files are
checklists (generated by son-of-anton) of exactly what was reviewed from each
set of commits affecting these parts of the code.

## Roadmap

- [x] Convert CoMPASS binary files to ROOT
- [x] Convert WaveDump binary files to ROOT (742 family digitizers only)
- [x] Convert SOLARIS DAQ (SOL) binary files to ROOT
- [ ] Convert CoMPASS CSV files to ROOT

## License

[MIT](LICENSE)
